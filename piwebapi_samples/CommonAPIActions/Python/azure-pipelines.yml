trigger:
  batch: true
  branches:
    include:
      - master
  paths:
    include:
      - '/piwebapi_samples/CommonAPIActions/Python'

pr:
  branches:
    include:
      - master
  paths:
    include:
      - '/piwebapi_samples/CommonAPIActions/Python'

schedules:
  - cron: '0 0 * * 0'
    displayName: Weekly build
    branches:
      include:
        - master
    always: true

variables:
  - template: '/miscellaneous/build_templates/variables.yml'
  - name: projPath
    value: '/piwebapi_samples/CommonAPIActions/Python'
  - name: CredFile
    value: test_config.ini
  - name: CredFileLibrary
    value: product-readiness.piwebapi_python.ini
  - name: analysisProject
    value: PIWebAPI_Python

jobs:
  - job: Tests
    pool:
      name: 00-OSIManaged-Test
      demands: COMPUTERNAME -equals $(buildAgent)
    steps:
      - template: '/miscellaneous/build_templates/config.yml'
        parameters:
          fileOriginal: $(CredFile)
          fileLibrary: $(CredFileLibrary)
          filePath: $(projPath)

      - template: '/piwebapi_samples/CommonAPIActions/Python/run-tests.yml'
        parameters:
          projectPath: $(Build.SourcesDirectory)$(projPath)

  - template: '/miscellaneous/build_templates/analysis.yml'
    parameters:
      language: python
